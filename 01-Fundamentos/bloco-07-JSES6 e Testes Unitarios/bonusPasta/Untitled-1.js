// return "ID não identificada"
  
  // if (detail === undefined ) "Informação indisponível";
  // return 'ok'
const jet = {
    name:1,
    car:2,
    as:'fodase',
}

for (let i in jet) {
    if (jet[i] === 1)  console.log(i); 
}

// professionalBoard[i].Object.keys[j]
// Object.keys(professionalBoard[j])